import { StyleSheet } from "react-native";

export default StyleSheet.create({
    heading: {
        fontSize: 50,
        fontWeight: "bold"
    },
    headingSmall: {
        fontSize: 30,
        fontWeight: "bold"
    },
    button: {
        backgroundColor: 'lightgray',
        borderRadius: 8,
        padding: 15,
    },
    container: {
        padding: 10
    }
});